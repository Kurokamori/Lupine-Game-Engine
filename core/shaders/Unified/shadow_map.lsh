#shader "ShadowMap"
#description "Shadow map depth rendering with alpha test"

#begin properties
    uniform mat4 u_ViewProjection;
    uniform mat4 u_Model;
    uniform sampler2D u_AlbedoTexture;
    uniform int u_HasAlbedoTexture = 0;
    uniform float u_AlphaCutoff = 0.0;
#end properties

#begin vertex
    #input vec3 a_Position : POSITION 0
    #input vec2 a_TexCoord : TEXCOORD0 1

    #output vec2 v_TexCoord

    void main() {
        v_TexCoord = a_TexCoord;
        VERTEX_OUTPUT = MUL(u_ViewProjection, MUL(u_Model, vec4(a_Position, 1.0)));
    }
#end vertex

#begin fragment
    #output vec4 FragColor

    void main() {
        if (u_HasAlbedoTexture == 1) {
            float alpha = SAMPLE(u_AlbedoTexture, v_TexCoord).a;
            if (alpha < u_AlphaCutoff) {
                DISCARD;
            }
        }
        FragColor = vec4(1.0);
    }
#end fragment
