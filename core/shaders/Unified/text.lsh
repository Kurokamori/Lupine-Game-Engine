#shader "Text"
#description "2D text rendering from font atlas"

#begin properties
    uniform mat4 u_ViewProjection;
    uniform sampler2D u_FontAtlas;
#end properties

#begin vertex
    #input vec3 a_Position : POSITION 0
    #input vec3 a_Normal : NORMAL 1
    #input vec2 a_TexCoord : TEXCOORD0 2
    #input vec4 a_Color : COLOR 3

    #output vec2 v_TexCoord
    #output vec4 v_Color

    void main() {
        v_TexCoord = a_TexCoord;
        v_Color = a_Color;
        VERTEX_OUTPUT = MUL(u_ViewProjection, vec4(a_Position, 1.0));
    }
#end vertex

#begin fragment
    #output vec4 FragColor

    void main() {
        float alpha = SAMPLE(u_FontAtlas, v_TexCoord).r;
        FragColor = vec4(v_Color.rgb, v_Color.a * alpha);
    }
#end fragment
